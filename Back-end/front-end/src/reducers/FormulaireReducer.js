const formulaire = [
    { sexe : 'Choissez votre sexe ',
    name: 'rahma'
    },
   
]

const formulaireReducer = (state = formulaire, action)=>{
    switch (action.type) {
        default:
      return state;
    }

}

export default formulaireReducer;